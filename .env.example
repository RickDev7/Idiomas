# Variáveis de ambiente do frontend (Vite). NUNCA commite chaves reais.

# Use o backend Gemini Live para conversa por voz (true/false)
VITE_USE_GEMINI_LIVE=false

# URL base do backend (proxy seguro). Em dev local, o backend roda em :8787
VITE_BACKEND_URL=http://localhost:8787
